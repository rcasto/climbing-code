{
  "name": "climbing-code",
  "version": "1.0.0",
  "description": "playing around with new web api's and tech",
  "main": "server.js",
  "scripts": {
    "commonBuild": "npm run clean && npm run move",
    "build": "npm run commonBuild && webpack -p --config webpack.config.js",
    "buildDev": "npm run commonBuild && webpack --config webpack.config.js",
    "move": "cd src && cp ./public/*.* ./public/styles/*.css ./public/images/*.png ../dist --parents",
    "start": "npm run build && node server.js",
    "startDev": "set DEBUG=* && set PORT=3000 && npm run buildDev && node server.js",
    "clean": "rm -rf dist/*"
  },
  "engines": {
    "node": "7.1.0"
  },
  "author": "Richie Casto",
  "license": "ISC",
  "dependencies": {
    "compression": "^1.6.2",
    "debug": "^2.3.2",
    "express": "^4.14.0",
    "uuid": "^3.0.0",
    "webrtc-adapter": "^2.0.6",
    "ws": "^1.1.1"
  },
  "devDependencies": {
    "css-loader": "^0.28.0",
    "style-loader": "^0.16.1",
    "webpack": "^2.4.1"
  }
}
